---
title: atomWithReset
---

引用: https://github.com/pmndrs/jotai/issues/41

```ts
function atomWithReset<Value>(
  initialValue: Value
): WritableAtom<Value, SetStateAction<Value> | typeof RESET>;
```

创建一个可以使用 [`useResetAtom`](use-reset-atom.mdx) hook 重置为其 `initialValue` 的原子。 它的工作方式与原始原子完全相同，但您也可以将其设置为特殊值 [`RESET`](reset.mdx)。 请参阅 [可重置原子](../guides/resettable.mdx) 中的示例。

## 示例

```js
import { atomWithReset } from "jotai/utils";

const dollarsAtom = atomWithReset(0);
const todoListAtom = atomWithReset([
  { description: "Add a todo", checked: false },
]);
```
